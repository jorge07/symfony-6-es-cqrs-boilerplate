framework:
    test: true
    session:
        storage_factory_id: session.storage.factory.mock_file
    messenger:
        transports:
            events: 'in-memory://'
            users: 'in-memory://'
            failed: 'in-memory://'